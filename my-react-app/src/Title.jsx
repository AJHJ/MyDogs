function Title(){

    return(
        //Here goes the HTML inside the head tag, the head tag isn't used here because it is already in the INDEX.HTML file
        <title>The Best</title>
        

    );
}

export default Title